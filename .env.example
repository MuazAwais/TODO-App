# Environment Variables Template
# Copy this file to .env.local and fill in your values
# .env.local is gitignored and will not be committed to the repository

# Database Configuration
# For local development, use a file-based SQLite database
# Format: file:./path/to/database.sqlite
# Example: file:./db.sqlite
DATABASE_URL=file:./db.sqlite

# Turso Auth Token (only needed for Turso cloud databases)
# Leave empty for local SQLite development
# Get this from: turso db tokens create your-database-name
# TURSO_AUTH_TOKEN=your-turso-auth-token-here

# JWT Secret (for token generation)
# Generate a secure random string for production
# You can use: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# API URL (optional)
# Only needed if your API is hosted on a different domain
# For local development, leave empty or use /api
# NEXT_PUBLIC_API_URL=/api

# Node Environment
# Automatically set by Next.js, but you can override if needed
# NODE_ENV=development
